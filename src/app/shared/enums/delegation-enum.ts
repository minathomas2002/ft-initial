export enum EDelegationStatus {
  ACTIVE = "active",
  CANCELLED = "cancelled",
  EXPIRED = "expired",
  UPCOMING   = "upcoming"
}

export enum EDelegationActions {
  CANCEL = 1,
  Update = 2,
  DELETE = 3
}
